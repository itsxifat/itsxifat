let mode="blue";
let color;

if (mode==="dark") {
    color="black";
} else if (mode==="blue") {
    color="blue";
} else if (mode==="pink") {
    color="pink";
} else if (mode==="red") {
    color="red";
}

console.log(color);

let age=18;

let result = age>=18 ? "adult" : "not adult";
console.log(result)